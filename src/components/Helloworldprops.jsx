import React from 'react';

const Helloworldprops = ({myfriend}) => {
    return (
        <section>
            <h1>Hello {myfriend}</h1>
        </section>
    );
};

export default Helloworldprops;